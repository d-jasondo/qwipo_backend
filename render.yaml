services:
  - type: web
    name: qwipo-api
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      # Note: Set DATABASE_URL in the Render dashboard (Settings > Environment)
      # Do not commit secrets in this file.
      # Optional: enable enhanced AI features if you have an API key
      # - key: OPENAI_API_KEY
